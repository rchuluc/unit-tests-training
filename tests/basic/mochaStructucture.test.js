describe('Test suite', () => {
  // This name must be descriptive, e.g. "The account must be created if the credentials are ok"
  it('Very descriptive case name', () => {
    console.log('Doing some tests');
  });

  // Using the .only on a test case you ensure that only this case is running inside the test suite
  // it.only('Run this, only', () => {
  //     console.log('Running only this test')
  // })
});
